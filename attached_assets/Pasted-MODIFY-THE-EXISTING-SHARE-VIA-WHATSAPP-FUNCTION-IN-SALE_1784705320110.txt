MODIFY THE EXISTING "SHARE VIA WHATSAPP" FUNCTION IN SALES INVOICE.

CURRENT PROBLEM:

Currently, when I click "Share via WhatsApp", the system first opens the device/browser Share Sheet.

Then I have to:

1. Select WhatsApp
2. Select/search the customer contact again
3. Then send the invoice PDF and text

I DO NOT WANT THIS PROCESS.

REMOVE THIS INTERMEDIATE SHARE SHEET FLOW FROM THE "SHARE VIA WHATSAPP" BUTTON.

==================================================
REQUIRED USER FLOW
==================================================

I need this exact workflow:

Sales Invoice
    ↓
Click "Share via WhatsApp"
    ↓
System automatically gets the customer's saved WhatsApp number
    ↓
Directly open THAT CUSTOMER'S WhatsApp chat
    ↓
Prepare the invoice message/text for that customer
    ↓
Prepare/share the correct Sales Invoice PDF
    ↓
User should only need to press SEND in WhatsApp
    ↓
Customer receives invoice details + invoice PDF

IMPORTANT:

DO NOT make the user:

- Select "Share"
- Select WhatsApp from the Share Sheet
- Search for WhatsApp
- Select a contact manually
- Search for the customer's number again
- Enter the customer's phone number again

The customer has already been selected in the Sales Invoice.

Therefore, the ERP already knows which WhatsApp number must be used.

==================================================
1. GET WHATSAPP NUMBER AUTOMATICALLY
==================================================

When "Share via WhatsApp" is clicked:

Get:

Sales Invoice
    ↓
Linked Customer
    ↓
Customer Details / Customer Master
    ↓
Saved WhatsApp Number

Automatically use that WhatsApp number.

Example:

Sales Invoice:
SI-2026-00125

Customer:
ABC Traders

Customer WhatsApp:
9876543210

The system must automatically resolve the number to the correct WhatsApp format, for example:

919876543210

Then directly target that customer's WhatsApp conversation.

Do NOT ask the user to select ABC Traders again.

The invoice already contains the customer relationship.

==================================================
2. DIRECTLY OPEN SPECIFIC CUSTOMER WHATSAPP CHAT
==================================================

For the existing WhatsApp button, do NOT use the generic Web Share API / navigator.share() as the primary action.

The current Share Sheet behavior is NOT wanted.

Instead, directly open the specific customer's WhatsApp conversation using the customer's saved WhatsApp number.

For example, where appropriate use an official WhatsApp deep-link mechanism such as:

https://wa.me/{CUSTOMER_NUMBER}?text={ENCODED_MESSAGE}

or the appropriate official WhatsApp app/deep-link mechanism for the environment.

The URL/message must be generated dynamically.

DO NOT hardcode the number.

Expected behavior:

Click "Share via WhatsApp"

→ WhatsApp opens

→ ABC Traders chat opens directly

NOT:

Click Share
→ Device Share Sheet
→ Select WhatsApp
→ Select ABC Traders

==================================================
3. PREFILL INVOICE TEXT
==================================================

The correct invoice text should already be prepared when the customer's WhatsApp chat opens.

Use the existing invoice-sharing message if already implemented.

Example:

Dear ABC Traders,

Please find your invoice details below.

Invoice No: SI-2026-00125
Invoice Date: 22/07/2026
Amount: ₹25,750

Thank you,
{Billing Warehouse/Outlet/Company Name}

All information must come dynamically from the actual invoice.

The user should NOT have to copy/paste the message.

==================================================
4. PDF REQUIREMENT
==================================================

The correct Sales Invoice PDF must also be available for sending.

Use the SAME existing PDF generated by:

"Download PDF"

Do not create a different WhatsApp invoice format.

The PDF must correspond exactly to the invoice being shared.

Example:

User clicks WhatsApp on:

SI-2026-00125

The system must use:

Invoice-SI-2026-00125.pdf

Never another invoice.

==================================================
5. CRITICAL TECHNICAL REQUIREMENT — DO NOT IMPLEMENT A FAKE SOLUTION
==================================================

There is an important distinction between:

A. Opening a specific WhatsApp chat with prefilled text

and

B. Opening a specific WhatsApp chat with BOTH a PDF file attachment and text already attached.

A standard wa.me / WhatsApp deep link can target a specific phone number and prefill text.

However, DO NOT assume that a normal wa.me URL can automatically attach a local PDF file.

Do not use navigator.share() if it causes this unwanted flow:

Share Sheet
→ Select WhatsApp
→ Select Customer

That is specifically NOT the required UX.

First inspect what is technically possible in the current ERP environment.

==================================================
6. TARGET EXPERIENCE
==================================================

The desired experience is:

[SHARE VIA WHATSAPP]

User clicks once.

System automatically knows:

Customer = ABC Traders
WhatsApp = 919876543210
Invoice = SI-2026-00125
Amount = ₹25,750
PDF = Invoice-SI-2026-00125.pdf

Then:

WhatsApp opens directly to:

ABC Traders / 919876543210

with invoice information prepared.

The goal is that the ERP user only needs to:

PRESS SEND

Do not make the ERP user select the customer again.

==================================================
7. IMPLEMENTATION OPTIONS
==================================================

Implement the best technically valid method according to the following priority.

-------------------------------
OPTION 1 — DIRECT SPECIFIC CHAT + PDF + TEXT
-------------------------------

If the installed WhatsApp environment/platform provides an officially supported mechanism to:

- Target a specific WhatsApp number
- Pre-attach the invoice PDF
- Include invoice text/caption
- Open that exact conversation ready for user confirmation

then use that supported mechanism.

The final screen should be:

Customer's WhatsApp Chat
+
Invoice PDF
+
Invoice Text

User clicks:

SEND

Do not show the generic Share Sheet first.

-------------------------------
OPTION 2 — WHATSAPP BUSINESS PLATFORM/API
-------------------------------

If ordinary browser WhatsApp/deep links cannot pre-attach a PDF to a specific phone number, DO NOT create an unreliable browser hack.

Prepare the architecture for official WhatsApp Business Platform/API document messaging.

The system should be able to:

1. Get customer's WhatsApp number automatically
2. Generate/retrieve invoice PDF
3. Upload/use the PDF through the approved WhatsApp Business mechanism
4. Target that exact customer number
5. Send invoice document with invoice information/caption according to WhatsApp's official API rules

IMPORTANT:

If WhatsApp Business API is required, clearly tell me before changing the architecture and list exactly what credentials/configuration are required.

Do NOT silently implement an unofficial automation solution.

-------------------------------
OPTION 3 — DIRECT CHAT WITH TEXT + SECURE PDF LINK
-------------------------------

If neither of the above can be implemented with the current setup, use this fallback:

Click "Share via WhatsApp"

→ Automatically open exact customer's WhatsApp chat

→ Prefill invoice details

→ Include a secure clickable invoice PDF link

Example message:

Dear ABC Traders,

Please find Invoice SI-2026-00125.

Invoice Date: 22/07/2026
Amount: ₹25,750

Invoice PDF:
{SECURE_PDF_LINK}

Thank you,
{Branch Name}

This fallback is preferable to showing the generic Share Sheet.

The customer should click the secure link and see/download the exact PDF.

==================================================
8. DO NOT USE THIS FLOW
==================================================

DO NOT implement:

Click WhatsApp
    ↓
Browser/device Share Sheet
    ↓
Select WhatsApp
    ↓
Select Customer
    ↓
Send

THIS IS THE CURRENT PROBLEM.

Remove this workflow from the dedicated WhatsApp button.

==================================================
9. KEEP NORMAL SHARE SEPARATE IF NEEDED
==================================================

If the ERP needs a generic sharing feature, keep it as a separate button:

"Share"

This generic Share button may use:

navigator.share()

and allow:

WhatsApp
Email
Telegram
Other apps

But the dedicated:

"Share via WhatsApp"

button must NOT behave like a generic Share button.

Dedicated WhatsApp button:

→ Direct customer WhatsApp conversation

Generic Share button:

→ Device Share Sheet

Keep these two functions separate.

==================================================
10. CUSTOMER NUMBER VALIDATION
==================================================

Normalize the customer's WhatsApp number before opening WhatsApp.

For example:

9876543210

should correctly become:

919876543210

if India is the applicable configured country.

If already:

+919876543210

do not duplicate +91.

If stored as:

919876543210

do not add 91 again.

Validate carefully.

If no WhatsApp number exists, show:

"WhatsApp number is not available for this customer. Please update Customer Details."

==================================================
11. WAREHOUSE / OUTLET SUPPORT
==================================================

The ERP uses Warehouse and Outlet-based billing.

Make sure the correct invoice is generated based on the billing location.

Example:

Kochi Outlet
    ↓
Customer ABC Traders
    ↓
SI-KOC-001
    ↓
Customer saved WhatsApp
    ↓
Share via WhatsApp
    ↓
ABC Traders WhatsApp directly
    ↓
SI-KOC-001 invoice information/PDF

Never mix invoices, customers, warehouses, or outlets.

==================================================
12. SECURITY
==================================================

Resolve the relationship securely:

Authenticated User
    ↓
Company
    ↓
Warehouse/Outlet
    ↓
Sales Invoice
    ↓
Customer
    ↓
Customer WhatsApp Number

Do not trust a manually manipulated customer phone number passed from the frontend.

The backend should verify that the customer is actually linked to the selected Sales Invoice.

If secure invoice PDF links are used:

- Do not expose local server file paths
- Do not expose unrestricted invoice URLs
- Use secure access-controlled or time-limited links where appropriate

==================================================
13. IMPORTANT: DO NOT AUTO-SEND USING UNOFFICIAL AUTOMATION
==================================================

The final SEND action can remain with the ERP user.

Desired:

Click WhatsApp in ERP
→ Correct customer WhatsApp opens directly
→ Message/document is ready
→ User presses SEND

Do NOT use browser bots, DOM manipulation, WhatsApp Web automation, Selenium, unofficial WhatsApp libraries, or hacks to automatically click SEND.

Use only supported mechanisms.

==================================================
14. FIRST ANALYZE CURRENT IMPLEMENTATION
==================================================

Before writing code, inspect:

- Current "Share via WhatsApp" button
- Whether it currently uses navigator.share()
- Current wa.me logic, if any
- Customer WhatsApp/mobile field
- Customer-to-Sales-Invoice relationship
- Existing PDF generation
- Existing PDF storage/download endpoint
- Mobile and desktop behavior

Identify exactly why the generic Share Sheet is opening.

Then change ONLY the dedicated WhatsApp workflow.

==================================================
15. FINAL ACCEPTANCE TEST
==================================================

Customer:

ABC Traders

WhatsApp:
9876543210

Invoice:
SI-001

Click:

"Share via WhatsApp"

EXPECTED:

The system automatically gets:

9876543210

→ Normalizes the number

→ Opens ABC Traders' WhatsApp conversation DIRECTLY

→ Invoice message is prepared automatically

→ Invoice PDF/document is provided using the best officially supported implementation

→ User only performs the final SEND action where technically supported.

NOT ACCEPTABLE:

Share Sheet appears
→ User selects WhatsApp
→ User searches/selects ABC Traders again

The customer must never need to be manually selected a second time.

CRITICAL:

DO NOT claim that wa.me can pre-attach a local PDF if WhatsApp does not support it.

If direct specific-chat + pre-attached PDF is impossible with the current browser architecture, implement direct specific-chat + secure invoice PDF link, OR clearly identify that WhatsApp Business Platform/API integration is required for true direct document delivery.

Do not return to the generic Share Sheet workflow.