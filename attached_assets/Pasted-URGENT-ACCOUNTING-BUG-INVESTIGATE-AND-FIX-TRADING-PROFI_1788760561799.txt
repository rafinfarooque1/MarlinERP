URGENT ACCOUNTING BUG: INVESTIGATE AND FIX TRADING & PROFIT/LOSS FORMULA

I have identified a serious issue in the current Trading and Profit & Loss Account.

Current displayed figures are:

Sales = ₹40,895.17
Sales Returns = ₹990.47
Net Sales = ₹39,904.70

Opening Stock = ₹18,489.54
Purchases (net of returns) = ₹29,060.00
Direct Expenses = ₹1,330.00
Goods Available for Sale = ₹48,879.54

Closing Stock = ₹58,650.11

Current system calculates:

COGS = ₹48,879.54 - ₹58,650.11
     = -₹9,770.57

Gross Profit = ₹39,904.70 - (-₹9,770.57)
            = ₹49,675.27

GP % = 124.5%

THIS IS NOT ACCEPTABLE.

The arithmetic is technically correct, but the accounting result indicates that the underlying inventory/COGS calculation is wrong or the closing stock valuation is inconsistent with the goods available for sale.

DO NOT simply change the UI formula or force COGS to zero.

INVESTIGATE THE ROOT CAUSE.

==================================================
1. VERIFY SALES
==================================================

Verify the current Sales calculation:

Gross Sales
LESS Sales Returns
= Net Sales

Confirm that:

₹40,895.17 - ₹990.47 = ₹39,904.70

Trace the amount directly to the underlying sales and sales-return transactions.

Verify:

- cancelled sales excluded
- cancelled returns excluded
- location filtering correct
- GST excluded from sales as intended
- Other Charges handled correctly
- credit notes/returns handled correctly
- no duplicate sales
- no duplicate sales return
- All Locations/company-wide aggregation correct

Do not change Sales if it is already correct.

==================================================
2. VERIFY CLOSING STOCK
==================================================

This is the most important investigation.

Current closing stock:

₹58,650.11

Determine exactly how this amount is calculated.

Trace it to:

- stock_entries
- stock_batches
- stock_ledger
- purchases
- purchase returns
- sales
- sales returns
- stock transfers
- stock adjustments
- opening stock
- production if applicable
- inventory valuation method
- item cost
- batch cost
- FIFO/FEFO
- weighted average if currently used

Produce an item-by-item reconciliation:

Item
Opening Qty
Opening Value
Purchases Qty
Purchase Value
Transfer In Qty/Value
Transfer Out Qty/Value
Sales Qty/Value
Sales Return Qty/Value
Purchase Return Qty/Value
Adjustments
Closing Qty
Closing Value
Cost Used

Find exactly why closing inventory is ₹58,650.11.

==================================================
3. VERIFY GOODS AVAILABLE FOR SALE
==================================================

Current:

Opening Stock = ₹18,489.54
Purchases = ₹29,060.00
Direct Expenses = ₹1,330.00

Total = ₹48,879.54

Verify each component independently from source transactions.

Do not trust the existing P&L result.

==================================================
4. VERIFY DIRECT EXPENSES
==================================================

Determine exactly what is included in:

Direct Expenses = ₹1,330.00

Inspect the account classification.

Only include a direct expense in inventory/COGS calculation if the existing business/accounting rules classify it as a direct cost attributable to inventory.

Do NOT include ordinary operating expenses in Goods Available for Sale.

Do not change account classification merely to make the P&L balance.

==================================================
5. CHECK THE FUNDAMENTAL INVENTORY EQUATION
==================================================

For the selected period/location/company:

Opening Inventory
+ Net Purchases
+ valid inventory-related direct costs
+ valid production/inventory adjustments
+ Transfer In where applicable
- Transfer Out where applicable
- Inventory consumed/sold
- Purchase Returns
+ Sales Returns
± valid inventory adjustments
=
Closing Inventory

Determine which architecture the ERP intentionally uses.

Do not double-count transfers.

Internal warehouse transfers must NOT create profit or loss.

Company-wide internal transfers must not increase total company inventory.

==================================================
6. CHECK INTERNAL STOCK TRANSFERS
==================================================

This is critical because the ERP has warehouse transfers.

Verify whether transfer-in inventory is being added to closing stock while also being included elsewhere.

For company-wide reporting:

Transfer Out and Transfer In must cancel at company level.

For individual warehouse reporting:

Transfer Out reduces source inventory.

Transfer In increases destination inventory.

Do NOT count the same inventory twice.

Check completed transfers, partial receipts, rejected transfers and reservations.

Also verify the known production issue:

Reservation ID 18
Transfer ID 9
Item ID 90
Warehouse ID 5
Quantity 5
Reservation active
Transfer completed

Verify whether this issue affects closing inventory or available stock.

Do not blindly delete the reservation.

==================================================
7. CHECK STOCK VALUATION
==================================================

Determine the ERP's intended inventory valuation method.

Verify whether it is:

- FIFO
- FEFO
- Weighted Average
- another documented method

The P&L inventory valuation must use the same authoritative inventory valuation logic as the inventory module.

Do NOT calculate closing stock using one method and COGS using another method.

Do NOT use current weighted-average cost to incorrectly reconstruct historical stock if historical cost data is available.

If historical inventory cannot be reliably reconstructed from existing data, explicitly identify the limitation instead of fabricating a value.

==================================================
8. CHECK HISTORICAL STOCK RECONSTRUCTION
==================================================

The previous forensic audit identified a potential architectural problem:

Historical inventory is reconstructed by rewinding current stock using stock ledger movements and current valuation.

Investigate whether this is causing the ₹58,650.11 closing stock to be overstated.

Check:

- deleted historical movements
- missing historical movements
- missing batches
- legacy records
- transfer receipt timing
- current-vs-historical weighted average cost
- stock ledger completeness
- opening stock

Do not rewrite historical data blindly.

==================================================
9. CORRECT TRADING ACCOUNT FORMULA
==================================================

The correct conceptual structure should be:

NET SALES
=
Sales
- Sales Returns
- applicable sales-side deductions

COGS
=
Opening Stock
+ Net Purchases
+ valid direct inventory costs
- Closing Stock

GROSS PROFIT
=
Net Sales
- COGS

However, do NOT simply implement this formula until the closing stock and purchase figures have been reconciled.

The formula must use authoritative values.

If COGS genuinely becomes negative because of a legitimate accounting situation, investigate and explicitly flag it rather than silently displaying an unrealistic GP.

For a normal trading business, a negative COGS combined with GP greater than Net Sales is a strong anomaly and must trigger investigation.

==================================================
10. DO NOT HIDE THE PROBLEM
==================================================

DO NOT do any of the following:

- force COGS to zero
- cap Gross Profit at Net Sales
- hide negative COGS
- hide closing stock
- manipulate GP percentage
- subtract arbitrary amounts
- change historical transactions just to make the report look normal
- change stock values without tracing source transactions

The goal is ROOT-CAUSE CORRECTION.

==================================================
11. RECONCILE WITH INVENTORY MODULE
==================================================

The Closing Stock value shown in P&L must reconcile with the Inventory Valuation report for the same:

- location
- date
- period

If Inventory module says one value and P&L says another, identify why.

There must be one authoritative inventory valuation calculation.

Do not maintain separate competing valuation formulas.

==================================================
12. RECONCILE WITH BALANCE SHEET
==================================================

Closing inventory shown in:

Trading/P&L
Inventory Report
Balance Sheet

must agree for the same date/location.

If they differ, trace the calculation path.

Do not solve discrepancies by manually changing one report.

==================================================
13. LOCATION TEST
==================================================

Test:

All Locations
Head Office
Warehouse A
Warehouse B

For each:

- Opening Stock
- Purchases
- Sales
- Sales Returns
- Transfers
- Closing Stock
- COGS
- Gross Profit

must obey location rules.

Company-wide internal transfers must not inflate total inventory.

==================================================
14. ACCOUNTING ENTRIES
==================================================

Verify the underlying journal entries.

Check whether:

Purchase
Sale
Sales Return
Purchase Return
Stock Transfer
Stock Adjustment

are creating the correct accounting entries.

Internal transfers must remain P&L neutral.

Do not create revenue/expense from internal warehouse movement.

==================================================
15. TEST A SIMPLE CONTROL CASE
==================================================

Create/use a safe test scenario in a non-production/test environment:

Opening stock = ₹10,000
Purchase = ₹5,000
Closing stock = ₹8,000
Net Sales = ₹12,000

Expected:

Goods Available = ₹15,000
COGS = ₹7,000
Gross Profit = ₹5,000

Verify the ERP produces the same result.

Then test:

Closing stock > goods available

and confirm the system does NOT silently generate an unrealistic 100%+ GP without detecting the underlying stock anomaly.

==================================================
16. REPORT VALIDATION
==================================================

After fixing the accounting engine, verify:

- Trading Account
- P&L
- Balance Sheet
- Trial Balance
- Inventory Valuation
- Stock Ledger
- Dashboard
- Month Wise report
- PDF
- CSV/export

All must use the same authoritative calculation.

Do not create a separate formula for PDF.

==================================================
17. ACCEPTANCE CRITERIA
==================================================

The current report:

Net Sales = ₹39,904.70
Closing Stock = ₹58,650.11
COGS = -₹9,770.57
Gross Profit = ₹49,675.27
GP = 124.5%

must NOT simply be accepted as correct.

Determine whether:

A. Closing stock is overstated,
B. Opening stock is wrong,
C. Purchases are incomplete,
D. purchases/returns are wrong,
E. sales/returns are wrong,
F. direct expenses are incorrectly classified,
G. transfers are double-counted,
H. stock valuation is wrong,
I. historical stock reconstruction is wrong,
J. or another root cause exists.

Give the exact root cause with transaction-level evidence.

Then implement the minimum safe root-cause fix.

==================================================
18. FINAL VERIFICATION
==================================================

After fixing:

1. Recalculate Sales.
2. Recalculate Net Sales.
3. Recalculate Opening Stock.
4. Recalculate Purchases.
5. Recalculate Direct Inventory Costs.
6. Recalculate Closing Stock.
7. Recalculate COGS.
8. Recalculate Gross Profit.
9. Recalculate Gross Profit %.
10. Compare P&L inventory with Inventory Valuation.
11. Compare P&L inventory with Balance Sheet.
12. Verify Trial Balance.
13. Verify location-wise results.
14. Verify All Locations result.
15. Verify internal transfers remain P&L neutral.
16. Verify known stale reservation issue.
17. Run regression tests.
18. Run production-safe reconciliation.

FINAL OUTPUT MUST STATE:

- Exact root cause
- Old calculation
- Correct calculation
- Correct closing stock
- Correct COGS
- Correct Gross Profit
- Correct GP %
- Transactions causing discrepancy
- Code files changed
- Database changes, if any
- Tests performed
- Test results
- Any historical data limitations

IMPORTANT:
Do not tell me merely "formula fixed".

I need the underlying transaction-level reason why Closing Stock ₹58,650.11 is greater than Goods Available for Sale ₹48,879.54.

START INVESTIGATION NOW.