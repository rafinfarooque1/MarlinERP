BUILD AND DISTRIBUTE THE ERP ANDROID APK DIRECTLY FROM THE PROJECT

IMPORTANT:
I do NOT want a manually configured APK URL.

I do NOT want an APK upload field.

I do NOT want Google Play Store publication.

I do NOT want Apple App Store publication.

I want Replit to BUILD the Android APK from the existing mobile application source code and make that generated APK directly downloadable from the ERP website.

============================================================
1. PRIMARY REQUIREMENT
============================================================

The existing ERP already has a mobile application.

Use the EXISTING mobile application source code.

Build a production Android APK from that source.

The APK must be generated by the project/build process.

Do NOT ask the Admin to:

- upload an APK
- enter an APK URL
- configure a download URL
- publish to Google Play Store

The application itself must provide the download experience.

============================================================
2. BUILD THE APK
============================================================

Inspect the existing mobile application first.

Determine whether it is:

- Expo
- React Native
- Expo Router
- React Native CLI
- another supported mobile framework

Use the existing framework.

Do NOT rewrite the mobile application.

Configure the project so that a production Android APK can be generated.

The final artifact must be:

.apk

NOT:

.aab

The requirement is specifically a directly installable Android APK.

============================================================
3. APK BUILD PROCESS
============================================================

Create a reliable production build command.

For example, depending on the existing framework:

npm run build:android
or
npm run android:apk

Use the appropriate command for the actual project.

If EAS Build is required, configure the project appropriately for an Android APK build.

The output must be a real installable APK.

Do not merely create a placeholder file.

============================================================
4. DO NOT USE PLAY STORE
============================================================

The APK must be distributed directly.

There must be NO requirement for:

Google Play Console
Google Play Store listing
Google Play publication

The user downloads the APK directly from the ERP website.

============================================================
5. WEBSITE PROFILE MENU
============================================================

Use the existing:

Profile icon
→ Download Mobile App

menu.

When the user clicks:

Download Mobile App

show:

DOWNLOAD MOBILE APP

Android App

Version: [current generated app version]

[ Download Android APK ]

The button must download the APK generated by the project.

============================================================
6. NO APK URL SETTING
============================================================

REMOVE the requirement for:

Android APK File Link

There must be no Admin field where someone has to paste:

https://....

The application knows where its generated APK is located.

The download route should point to the project's current generated APK artifact.

============================================================
7. NO APK UPLOAD
============================================================

Do NOT create:

Upload APK

Choose APK

Replace APK

APK File Upload

or similar settings.

The APK comes from the project's build process.

============================================================
8. AUTOMATIC APK ARTIFACT
============================================================

After a successful production Android build:

1. Generate APK.
2. Store/copy the generated APK into the application's appropriate deployment artifact/static-download location.
3. Make it available through the ERP's authenticated/public download route as appropriate.
4. The Profile → Download Mobile App button must automatically point to the current generated APK.

The production deployment must be able to serve the APK.

Do not rely on a temporary local development path.

============================================================
9. VERSION
============================================================

The Android app version should come from the mobile application's actual version configuration.

Do NOT require a separate manual "Android App Version" setting in ERP Settings.

Example:

Mobile app version:
1.0.0

The download modal can display:

Android App
Version 1.0.0

The version must remain synchronized with the mobile app build.

============================================================
10. BUILD ARTIFACT MANAGEMENT
============================================================

Do not commit large generated APK binaries into source control unless the existing deployment architecture specifically requires it.

Prefer the project's supported build/deployment artifact mechanism.

The important requirement is:

SOURCE CODE
→ BUILD
→ APK ARTIFACT
→ ERP DOWNLOAD ENDPOINT
→ USER DOWNLOAD

The APK must survive normal deployment/restart according to the chosen deployment architecture.

============================================================
11. DOWNLOAD ENDPOINT
============================================================

Create a dedicated backend download endpoint, for example:

/api/mobile/android-apk

or another appropriate route.

The endpoint must return the actual APK file.

It must NOT return:

HTML
JSON
a development page
a missing-file response

Use the correct APK content type and download headers.

Use a clean filename such as:

CompanyERP-Mobile-1.0.0.apk

============================================================
12. SECURITY
============================================================

Decide appropriately whether the APK itself needs authentication.

The download route must NOT expose arbitrary filesystem access.

The user must never be able to change a filename/path parameter to download unrelated server files.

Only the intended APK artifact can be downloaded.

============================================================
13. PROFILE DOWNLOAD FLOW
============================================================

Final user experience:

ERP Website

→ Click Profile Icon

→ Download Mobile App

→ Android

→ Download Android APK

→ APK downloads

→ User installs APK on Android phone.

No URL configuration.

No APK upload.

No Play Store.

============================================================
14. QR CODE
============================================================

The Download Mobile App modal may include a QR code.

The QR code must automatically point to the ERP's APK download endpoint.

Do NOT require manual QR configuration.

Example:

Scan QR
→ ERP APK download endpoint
→ APK downloads

Do not hardcode localhost.

Use the actual production ERP domain.

============================================================
15. MOBILE APP BACKEND
============================================================

The generated APK must continue using the existing ERP backend/API.

Do NOT create a separate backend.

Preserve:

Authentication
Permissions
Location restrictions
Dashboard
POS
Receipt Voucher
Payment Voucher
Accounting
GST
Payroll
All existing mobile functionality

============================================================
16. ANDROID INSTALLATION
============================================================

The downloaded file must be a normal Android APK that the user can install.

Android may display a security warning because the APK is being installed outside Google Play.

That is expected.

Do not attempt to bypass Android security.

The website can display a short instruction such as:

"Download the APK and open it on your Android phone to install the app. Android may ask you to allow installation from this source."

============================================================
17. AUTOMATED BUILD / DEPLOYMENT
============================================================

Configure the project so the APK can be regenerated whenever the mobile application is released.

The build process should:

1. Build the mobile app.
2. Generate APK.
3. Verify APK exists.
4. Verify APK is non-empty.
5. Make the APK available to the ERP download endpoint.

If the build fails:

DO NOT replace the existing working APK with an invalid artifact.

============================================================
18. IMPORTANT — DO NOT BREAK THE WEBSITE
============================================================

The website must continue working even if an APK has not yet been generated.

If no APK exists:

Show:

"Android app download is temporarily unavailable."

Do not show a broken download button.

Once the APK exists:

Automatically show:

[ Download Android APK ]

============================================================
19. REMOVE THE CURRENT SETTINGS
============================================================

The current Mobile App Settings containing:

Android APK File Link
Android App Version
iOS Installation Link
iOS App Version

should be redesigned.

For Android:

REMOVE manual APK URL configuration.

REMOVE APK upload.

REMOVE manual APK version configuration.

The app version should come from the mobile project's version/build configuration.

For iOS:

Do not create an APK-equivalent workflow.

============================================================
20. IOS IMPORTANT LIMITATION
============================================================

DO NOT attempt to install an Android APK on iOS.

DO NOT generate an IPA and pretend that users can universally download and install it directly from the ERP website.

iOS requires Apple's supported signing/distribution mechanisms.

For THIS task:

IMPLEMENT ANDROID APK DIRECT DISTRIBUTION.

Do not block or redesign the Android implementation because iOS is different.

If an iOS option remains in the UI, clearly show that iOS distribution is not configured rather than displaying a fake download button.

============================================================
21. TESTING
============================================================

After implementation:

A. Build the Android app.

B. Confirm a real .apk is generated.

C. Confirm APK is non-empty.

D. Confirm it is a valid Android APK.

E. Install it on an Android test device/emulator where possible.

F. Open the app.

G. Confirm login works.

H. Confirm ERP API connection works.

I. Confirm mobile dashboard works.

J. Confirm Receipt Voucher works.

K. Confirm Payment Voucher works.

L. Confirm location permissions work.

M. Confirm warehouse restrictions work.

N. Open ERP website.

O. Click Profile.

P. Click Download Mobile App.

Q. Click Download Android APK.

R. Confirm the SAME generated APK downloads.

S. Scan QR code and confirm it downloads the same APK.

============================================================
22. FINAL ACCEPTANCE CRITERIA
============================================================

The final architecture must be:

EXISTING MOBILE SOURCE CODE
        ↓
ANDROID PRODUCTION BUILD
        ↓
GENERATED .APK
        ↓
ERP DOWNLOAD ENDPOINT
        ↓
PROFILE ICON
        ↓
DOWNLOAD MOBILE APP
        ↓
DOWNLOAD ANDROID APK
        ↓
INSTALL ON ANDROID

There must be:

NO manual APK URL
NO APK upload
NO Google Play Store
NO Play Store dependency
NO fake download link

The APK must be generated from the actual mobile application source code.

============================================================
23. FINAL REPORT
============================================================

After implementation report:

- Mobile framework detected
- Android build method used
- APK build command
- APK version
- APK artifact location
- Download endpoint
- Profile menu integration
- QR integration
- APK validation result
- Android installation test result
- Mobile API connection test
- Existing mobile feature regression results

If APK generation cannot be completed because of a missing build credential, signing configuration, or external build requirement:

DO NOT fake completion.

Clearly report the exact blocker and the minimum required configuration.

Do not substitute a placeholder APK.

============================================================
FINAL GOAL

I want the ERP developer/build system to generate the Android APK itself.

I do not want an Admin uploading or configuring the APK.

I do not want a Play Store listing.

The user should simply:

OPEN ERP
→ PROFILE
→ DOWNLOAD MOBILE APP
→ DOWNLOAD APK
→ INSTALL ON ANDROID

Make this the final user experience.