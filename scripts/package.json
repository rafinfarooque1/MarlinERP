{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsx ./src/check-permissions.ts",
    "permissions": "tsx ./src/check-permissions.ts",
    "permissions:write": "tsx ./src/check-permissions.ts --write"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
